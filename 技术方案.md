# zsttSystem 技术提升评估与实施方案

## 1. 方案目标

根据导师建议，项目可在现有 RAG-KG 基础上增加两项能力：

1. 以 Mermaid 展示课程间前置依赖，并进一步生成目标课程的选课路径。
2. 让显式选择的 persona 同时影响回答生成、检索排序和实验评估。

建议将两项能力在论文中分别定位为：

- **课程依赖图与路径规划**：系统能力与演示亮点。
- **Persona-aware RAG**：主要研究问题和论文贡献点。

不建议把两项工作都描述成简单的界面增强。依赖图的难点是可靠关系数据，
persona 的难点是可复现、可衡量的检索与回答差异。

### 1.1 总体评估

| 提升点 | 工程可行性 | 论文价值 | 当前主要阻塞 | 建议 |
|---|---|---|---|---|
| Mermaid 课程依赖图 | 中；渲染高、数据低 | 中高 | 当前没有课程依赖边，图谱存在旧节点 | 先建依赖数据，再做二跳图 |
| 选课路径规划 | 中 | 高于单纯画图 | 需要可靠 DAG、专业方案和学期约束 | 作为建议一第二阶段 |
| Persona prompt-only | 高 | 低到中 | 需要统一角色规范 | 用作演示与实验基线 |
| Persona-aware retrieval | 高 | 高 | 需要评估集和消融设计 | 作为论文主要贡献 |
| Persona-aware HyDE | 中 | 结果未知 | 当前没有 HyDE，增加成本和幻觉风险 | 仅作为可选消融 |

---

## 2. 当前系统基础评估

### 2.1 已具备的基础

当前项目已经具备：

- FastAPI 查询接口及统一 `QueryResponse`。
- ChromaDB 中的 `source_type`、课程代码、章节类型等元数据。
- 课程大纲与培养方案的分源检索。
- 向量分数、词面覆盖和章节类型组成的轻量重排。
- 结构化证据项及稳定的模板化降级回答。
- Neo4j Community 本地图数据库。
- `query_log.jsonl` 和反馈日志。
- 后端 concept-level Mermaid 字符串生成函数。

这些基础使 Persona 第一层和前端 Mermaid 渲染本身都比较容易。

### 2.2 课程依赖数据的实际缺口

2026-07-26 对当前本地数据的检查结果：

| 项目 | 数量 |
|---|---:|
| Course 节点 | 155 |
| Concept 节点 | 4564 |
| Chunk 节点 | 3249 |
| `TEACHES` 关系 | 4484 |
| `CONTAINS` 关系 | 3297 |
| 课程 `PREREQUISITE_OF` 关系 | 0 |

`outputs/courses.json` 中 155 门课程的 `prerequisites` 均为空，因此当前
`graph_builder.py` 虽然支持写入 `PREREQUISITE_OF`，实际上没有课程依赖边
可以写入。

部分课程大纲正文中已有明确先修信息，例如：

```text
信息组织基础
先修课程：信息管理学基础
```

目前在线路由通过正则从 ChromaDB 临时提取该字段回答问题，但没有把它规范化
为课程代码，也没有持久化到课程目录和 Neo4j。

此外，当前 `dependency_explainer.py` 中的 Mermaid 和路径查询面向另一套
`Knowledge_Point + REQUIRES/FOUNDATION_OF/...` 模型；当前离线管线实际写入
的是 `Concept + TEACHES/CONTAINS`。两套模型尚未完全接通。

因此：

> Mermaid 前端渲染本身可在 1–2 天完成，但“课程前置依赖图”不能直接建立在
> 当前图数据上。必须先完成依赖字段提取、课程实体对齐和图谱同步。

### 2.3 Neo4j 同步问题

当前 ChromaDB/JSON 中有 2677 个课程块，而 Neo4j 中有 3249 个 Chunk 节点。
`write_neo4j` 使用 `MERGE` 写入新节点，但没有删除已经不存在的旧 Chunk 和
Concept 节点，说明图数据库存在历史残留。

依赖图开发前应先让图谱写入具备真正的快照一致性，否则可视化可能显示已经
从源数据删除的节点和关系。

### 2.4 Persona 与 HyDE 的实际状态

当前系统：

- `QueryRequest` 只有 `query`。
- 前端没有角色选择控件。
- 路由器使用固定的 syllabus/training_plan 配额和章节权重。
- 生成器只有统一提示词。
- 查询日志没有 persona、实验组和检索配置。
- 项目中没有 `_hyde_expand` 或其他 HyDE 实现。

代码中存在一处“LLM client used for HyDE”的历史注释，但不是可调用能力。
因此 HyDE 应作为后续可选实验，不应计入 Persona 第一层工作量。

---

## 3. 建议一：课程依赖图与路径规划

### 3.1 设计原则

1. **边列表是事实来源，Mermaid 只是展示格式。**
2. **课程硬先修和概念相关性必须分开。**
3. **不让 LLM 生成图结构。**
4. **默认只展示查询课程的有限邻域。**
5. **无法形成 DAG 时不生成虚假的选课顺序。**

建议使用以下关系：

| 关系 | 含义 | 是否参与拓扑排序 |
|---|---|---|
| `PREREQUISITE_OF` | 大纲明确声明的硬先修 | 是 |
| `RECOMMENDED_BEFORE` | 教学内容推断或教师建议的软先修 | 可选 |
| `CONCEPTUAL_BASIS` | 概念层知识基础 | 否 |
| `TEACHES` | 课程讲授某概念 | 否 |

只有经过课程实体对齐、来源可追踪的 `PREREQUISITE_OF` 才默认参与选课规划。

### 3.2 阶段 A：建立课程依赖数据

#### 3.2.1 提取显式先修字段

修改 `parser_chunker.py`：

- 从课程基本信息表中提取 `先修课程`、`前置课程`、`预修课程`。
- 结构化保存原始文本，例如 `信息管理学基础`。
- 将字段回写到课程目录，而不只保留在 chunk 正文中。
- 记录证据文件、章节、原始文字和来源年份。

建议数据形状：

```json
{
  "course_code": "IM2105",
  "course_name": "信息组织基础",
  "prerequisites": [
    {
      "raw_name": "信息管理学基础",
      "course_code": "IM121",
      "relation": "PREREQUISITE_OF",
      "confidence": 1.0,
      "source_type": "syllabus",
      "source_file": "IM2105信息组织基础_转自DOC.docx",
      "section": "课程基本信息",
      "verified": true
    }
  ]
}
```

#### 3.2.2 课程实体对齐

构建课程名称到代码的规范化索引：

- 完整中文名精确匹配。
- 课程代码精确匹配。
- 中英文名称和人工维护别名匹配。
- 去除“基础”“原理”等词后的模糊匹配不能直接成为硬先修。

未能唯一对齐的名称写入：

```text
outputs/unresolved_prerequisites.json
```

由人工确认后再进入 Neo4j。不要使用 LLM 猜测课程代码后直接写图。

#### 3.2.3 图谱快照同步

每次 graph stage 生成 `build_id`，所有本次写入的节点和边附带该 ID。写入结束
后删除不属于当前 build 的 Chunk、Concept 和派生关系，或在事务中先清理
受管关系后完整重写。

建议至少保证：

- `outputs/chunks.json` 的 chunk 数与 Neo4j Chunk 数一致。
- 已删除课程资料不会继续出现在 Neo4j。
- `PREREQUISITE_OF` 每条边都能追溯到来源。

### 3.3 阶段 B：课程子图 API

新增课程依赖服务，例如：

```text
src/online_service/course_dependency_service.py
```

新增接口：

```http
GET /courses/{course_code}/dependencies?depth=2&max_nodes=30
```

或在 dependency 查询命中明确课程时填充相同结构。

推荐响应：

```json
{
  "target_course": "IM2105",
  "nodes": [
    {
      "id": "IM121",
      "course_code": "IM121",
      "label": "信息管理学基础",
      "semester": 1,
      "distance": 1
    },
    {
      "id": "IM2105",
      "course_code": "IM2105",
      "label": "信息组织基础",
      "semester": 3,
      "distance": 0
    }
  ],
  "edges": [
    {
      "source": "IM121",
      "target": "IM2105",
      "relation": "PREREQUISITE_OF",
      "confidence": 1.0
    }
  ],
  "truncated": false,
  "plan": null
}
```

课程依赖关系的方向统一为：

```text
先修课程 --PREREQUISITE_OF--> 后续课程
```

#### 3.3.1 Cypher 邻域查询

当前实际关系名是 `PREREQUISITE_OF`，不是 `DEPENDS_ON`。二跳查询示例：

```cypher
MATCH p=(neighbor:Course)-[:PREREQUISITE_OF*1..2]-(
  center:Course {course_code: $course_code}
)
RETURN p
LIMIT 100
```

后端只允许预定义的 `depth` 值，例如 1、2、3，再选择对应查询模板，避免将
未校验的长度直接拼入 Cypher。

#### 3.3.2 子图裁剪

默认：

- `depth=2`
- `max_nodes=30`

节点排序优先级：

1. 查询目标课程。
2. 距离更近的硬先修课程。
3. 有明确来源的关系。
4. 关系置信度更高。
5. 培养方案中开课学期更接近目标课程。

超过上限时返回 `truncated=true` 和实际总数，前端提示用户调整深度，而不是
静默漏掉节点。

### 3.4 阶段 C：Mermaid 渲染

推荐由后端返回规范化 nodes/edges，前端确定性生成 Mermaid，不再让 LLM
返回 Mermaid 源码。

示例：

```mermaid
graph LR
    IM121["信息管理学基础"] -->|先修| IM2105["信息组织基础"]
```

节点 ID 使用课程代码：

```text
IM121
IM2105
```

中文只作为 label。对于非字母数字 ID，使用固定前缀和哈希：

```text
C_a18f29["课程中文名"]
```

前端实现要求：

- 固定 Mermaid 版本并将资源放入项目静态目录，避免演示依赖公共 CDN。
- 使用 `securityLevel: "strict"`。
- 对 label 中的引号、方括号和换行进行转义。
- 节点超过 30 个时不渲染，显示裁剪提示。
- Mermaid 渲染失败时保留可访问的课程边列表。

建议在 `demo.html` 增加：

- “依赖关系图”区域。
- 深度选择 1/2/3。
- “仅显示硬先修”开关。
- 原始边列表的折叠备用视图。

### 3.5 阶段 D：选课路径规划

路径规划不建议直接依赖 Neo4j 返回一条“最长路径”，因为一个目标课程可能有
多个并行先修分支。更稳妥的方案是：

1. Neo4j 返回目标课程的全部先修祖先子图。
2. Python 检查环。
3. 使用 Kahn 算法拓扑排序。
4. 使用最长前驱深度生成学习层级。
5. 结合培养方案开课学期展示建议时间。

层级计算：

```text
layer(course) = max(layer(prerequisite) + 1)
```

输出示例：

```json
{
  "layers": [
    {
      "stage": 1,
      "courses": ["IM121"]
    },
    {
      "stage": 2,
      "courses": ["IM2105"]
    }
  ],
  "warnings": [],
  "is_dag": true
}
```

注意：

- `stage` 是依赖层级，不应直接伪装成学校真实学期。
- 有培养方案 semester 时，可额外显示“官方开课学期”。
- 同一课程属于多个培养方案时，必须先选择专业/方案。
- 检测到环时返回环路径并停止生成选课顺序。
- 软先修默认不阻塞拓扑排序，只作为建议显示。

### 3.6 建议一验收标准

#### 数据

- 所有显式先修文本均进入解析结果。
- 可唯一匹配的先修课程代码解析准确率不低于 95%。
- 未解析名称进入人工审核文件。
- 每条硬先修边都有来源证据。
- 图谱不存在未说明的硬先修环。

#### API

- 默认只返回二跳、最多 30 个节点。
- edges 均满足统一方向和字段契约。
- 裁剪、无依赖、环和 Neo4j 不可用均有明确状态。

#### 前端

- 中文标签和特殊字符不会破坏 Mermaid。
- 30 节点以内可稳定渲染。
- 失败时仍可查看边列表。

#### 路径规划

- 并行先修关系不会被错误串行化。
- 所有硬先修均排在目标课程之前。
- 多培养方案场景不会混用学期信息。

### 3.7 阶段 A、B 实施结果（2026-07-26）

阶段 A、B、C、D 均已按本方案实现。

#### 阶段 A

- `parser_chunker.py` 已从课程基本信息中抽取“先修课程 / 前置课程 /
  预修课程”。
- 课程代码、完整中文名、英文名和人工别名只做规范化后的精确匹配；模糊匹配
  不会生成硬先修边。
- `courses.json.prerequisites` 保存已对齐课程代码，
  `prerequisite_evidence` 保存原始名称、来源文件、章节、年份、置信度和
  `verified` 状态。
- 未唯一对齐的数据写入 `outputs/unresolved_prerequisites.json`。
- Neo4j 写入改为单事务完整快照，并为本次节点和关系写入同一 `build_id`；
  旧批次 Chunk、Concept 和派生关系不再残留。

真实数据重建结果：

| 项目 | 数量 |
|---|---:|
| 课程总数 | 155 |
| 具有已对齐先修关系的课程 | 19 |
| `PREREQUISITE_OF` | 23 |
| 待人工确认名称 | 19 |
| Neo4j Course | 155 |
| Neo4j Concept | 4040 |
| Neo4j Chunk | 2677 |

Neo4j Chunk 数已与 `outputs/chunks.json` 的 2677 条完全一致。

#### 阶段 B

新增：

```http
GET /courses/{course_code}/dependencies?depth=2&max_nodes=30
```

实现约束：

- `depth` 只允许 1、2、3，并使用三份固定 Cypher 模板。
- `max_nodes` 范围为 1–30，默认 30。
- 查询目标课程始终优先保留，其后按距离、是否为目标课程的先修祖先、证据
  验证状态、置信度和开课学期距离确定性排序。
- 裁剪后只保留两端节点均存在的边，并返回 `truncated` 与 `total_nodes`。
- 响应保留 `plan: null`，供阶段 D 路径规划以后兼容扩展。
- 明确的先修问答在 Neo4j 可用时，会把同一子图结构写入
  `dependency_info`。

以 IM2105“信息组织基础”为目标的二跳实测返回 4 个节点、3 条硬先修边，
每条边均包含来源文件和章节。

质量验证：

```text
Ruff: all checks passed
Pytest: 38 passed
```

### 3.8 阶段 C、D 实施结果（2026-07-26）

#### 阶段 C：Mermaid 课程依赖图

- 固定 Mermaid 11.16.0 并保存到 `src/static/vendor/`，同时保留上游 MIT
  License；页面不依赖公共 CDN。
- FastAPI 通过 `/static` 提供本地脚本。
- 前端只使用后端的规范化 nodes/edges 确定性生成 `graph LR`，不让 LLM
  生成 Mermaid。
- 课程代码只含字母数字时直接作为节点 ID；其他 ID 使用稳定哈希前缀。
- 中文 label 与引号、方括号、尖括号、换行均经过转义。
- Mermaid 使用 `securityLevel: "strict"`。
- 页面提供 1/2/3 跳选择和“仅显示硬先修”状态。
- 超过 30 个节点不渲染；裁剪时明确显示原始节点总数。
- Mermaid 未加载或渲染失败时显示可读提示，课程边列表始终保留。

#### 阶段 D：选课路径规划

- 展示子图继续受 `depth/max_nodes` 限制；规划器单独读取完整
  `PREREQUISITE_OF` 图，避免因展示裁剪漏掉更早的先修课。
- 从目标课程反向筛选全部硬先修祖先。
- 使用 Kahn 算法检查 DAG，并以最长前驱层级生成并行学习阶段。
- 检测到环时返回环路径并停止输出学习层级。
- `stage` 明确标注为学习层级；培养方案学期单独保存为
  `official_semester`。
- 检查先修课开课学期是否早于后续课，冲突时返回 warning。
- 目标课程属于多个培养方案时返回 `program_required` 和可选方案；只有通过
  `program_name` 选择后才填充路径学期，不跨方案混用。
- 无硬先修关系时返回 `no_prerequisites`。

IM2105 实测属于三个培养方案，因此未选择方案时正确返回
`program_required`。选择“信息管理学院2025级信息管理与信息系统专业培养方案”
后，规划结果为：

```text
学习层级 1：IM121 信息管理学基础（官方第 1 学期）
学习层级 2：IM2105 信息组织基础（官方第 3 学期）
```

### 3.9 建议一验收记录（2026-07-26）

#### 数据验收

| 验收项 | 检查结果 | 状态 |
|---|---|---|
| 显式先修文本进入解析结果 | 42/42 个非空显式项已进入已解析证据或待审核清单 | 通过 |
| 可唯一匹配课程代码准确率 | 23/23 均为精确别名匹配，自动审计 100% | 通过 |
| 未解析名称进入审核文件 | 19 项位于 `unresolved_prerequisites.json` | 通过 |
| 硬先修边可追溯 | 23/23 条边均有 `verified=true` 和来源文件 | 通过 |
| 无未说明硬先修环 | Neo4j 1–10 跳闭环检查为 0；Python DAG 检查通过 | 通过 |
| 图谱快照一致 | Course 155、Concept 4040、Chunk 2677；Chunk 与 JSON 相同 | 通过 |

#### API 验收

| 验收项 | 检查结果 | 状态 |
|---|---|---|
| 默认二跳、最多 30 节点 | `depth=2`、`max_nodes=30`，并有参数边界测试 | 通过 |
| edges 统一契约 | 全部为先修 → 后续，relation 为 `PREREQUISITE_OF` | 通过 |
| 裁剪状态 | 返回 `truncated`、`total_nodes`；确定性裁剪测试通过 | 通过 |
| 无依赖 | 返回 `no_prerequisites` | 通过 |
| 环 | 返回 `cycle_detected`、环路径且不生成 layers | 通过 |
| Neo4j 不可用 | 接口返回明确 503，问答链路保留降级说明 | 通过 |

#### 前端验收

| 验收项 | 检查结果 | 状态 |
|---|---|---|
| 中文与特殊字符 | 浏览器实测中文、引号和方括号 label 成功生成 SVG | 通过 |
| 30 节点以内渲染 | IM2105 实测成功生成 Mermaid SVG | 通过 |
| 本地固定依赖 | Mermaid 11.16.0 本地加载，无 CDN | 通过 |
| 严格安全模式 | `securityLevel: "strict"` | 通过 |
| 失败降级 | 模拟 Mermaid 不可用，提示可见且边列表保留 1 项 | 通过 |

#### 路径规划验收

| 验收项 | 检查结果 | 状态 |
|---|---|---|
| 并行先修不串行化 | A/B → C → T 测试输出 `[A,B] / [C] / [T]` | 通过 |
| 硬先修均早于目标 | IM2105 实测所有相关边满足前层 < 后层 | 通过 |
| 多培养方案不混用 | 未选时 `program_required`；选定后只读取该方案学期 | 通过 |
| 学期异常提示 | 先修学期不早于后续学期时 warning 测试通过 | 通过 |

最终质量检查：

```text
Ruff: all checks passed
Pytest: 43 passed
Browser: Mermaid SVG rendered; fallback edge list retained
```

---

## 4. 建议二：Persona-aware RAG

### 4.1 研究问题

建议将论文问题定义为：

> 在使用同一课程知识库的条件下，让显式 persona 同时影响检索证据选择和回答
> 组织方式，是否能提高角色契合度，同时保持事实忠实度和引用正确性？

persona 由用户主动选择：

- `student`
- `teacher`
- `visitor`

不根据历史交互推断身份，不构建用户画像，不训练个人偏好模型。

### 4.2 Persona 行为定义

| Persona | 主要需求 | 优先证据 | 回答风格 |
|---|---|---|---|
| student | 学什么、难点、先修、学习顺序 | 课程目标、教学进度、先修课程 | 通俗、行动导向、解释术语 |
| teacher | 教学目标、内容覆盖、考核、培养方案对应 | 培养方案、课程目标、考核方式、教学进度 | 专业、结构化、保留教学术语 |
| visitor | 专业概况、课程定位、基本信息 | 培养方案、课程简介、基本信息 | 简洁、少术语、先解释背景 |

persona 只改变证据优先级、内容深度和组织方式，不能改变事实。

### 4.3 阶段 A：Prompt-only 基线

#### 4.3.1 API 契约

修改 `QueryRequest`：

```python
class QueryRequest(BaseModel):
    query: str
    persona: Literal["student", "teacher", "visitor"] = "student"
```

响应 metadata 增加：

```json
{
  "persona": "student",
  "persona_mode": "prompt"
}
```

旧客户端不传 persona 时默认 `student`，保证接口兼容。

如果希望严格保持当前中性回答，可将默认角色设为 `visitor`；但项目主要服务
学生，建议默认 `student`，并在论文中说明。

#### 4.3.2 前端

在问题输入框上方增加明确角色选择：

```text
我是：学生 / 教师 / 访客
```

使用下拉框或单选按钮，不自动推断。

#### 4.3.3 生成提示词

在 `generator.py` 中建立固定 persona 配置，不把前端任意文本直接拼入 system
prompt。

学生示例：

```text
面向学生回答。优先解释课程主要内容、先修基础、学习难点和建议顺序。
第一次出现的专业术语用一句通俗语言解释。
```

教师示例：

```text
面向教师回答。优先说明课程目标、教学内容、考核方式及其与培养方案的对应。
保留必要教学设计术语，并明确证据来源。
```

访客示例：

```text
面向非专业访客回答。先说明课程或专业定位，减少未解释术语，不展开内部教学细节。
```

模板化降级回答也必须使用 persona 配置，避免只有 LLM 正常时才体现角色。

### 4.4 阶段 B：检索感知 Persona

当前 `_retrieve_course_evidence` 固定检索：

- syllabus 10 条。
- training_plan 2 条。
- 课程目标、教学进度、教学内容获得统一偏好。

建议改为 persona profile：

```python
PERSONA_PROFILES = {
    "student": {
        "syllabus_top_k": 10,
        "plan_top_k": 2,
        "preferred_sections": [
            "course_objectives",
            "teaching_schedule",
            "prerequisites",
        ],
    },
    "teacher": {
        "syllabus_top_k": 8,
        "plan_top_k": 5,
        "preferred_sections": [
            "course_objectives",
            "assessment",
            "teaching_schedule",
        ],
    },
    "visitor": {
        "syllabus_top_k": 4,
        "plan_top_k": 5,
        "preferred_sections": [
            "overview",
            "basic_info",
            "course_objectives",
        ],
    },
}
```

#### 4.4.1 重排分数

将当前分数扩展为：

```text
final_score =
    0.75 × vector_score
  + 0.15 × lexical_score
  + section_boost(persona)
  + source_boost(persona)
```

示例：

- teacher 对 `training_plan` 增加来源权重。
- student 对 `course_objectives`、`teaching_schedule`、`prerequisites`
  增加权重。
- visitor 对 `overview`、`basic_info` 增加权重。

具体权重必须通过验证集选择，不能直接把上述示例当成最终实验参数。

#### 4.4.2 路由影响

persona 不应完全覆盖问题意图，但可调整次级策略：

- student 的“怎么选”进入依赖和路径规划。
- teacher 的“对应关系”优先组合 syllabus 与 training_plan。
- visitor 的宽泛专业问题优先 catalog/overview。

所有 persona 仍共享事实查询的结构化字段，避免相同问题得到互相矛盾的学分或
学时。

### 4.5 阶段 C：Persona-aware HyDE（可选）

当前项目没有 HyDE 实现。建议在 prompt-only 和 retrieval-aware 两层完成且有
稳定评估后再加入。

接口：

```python
def hyde_expand(query: str, persona: Persona) -> str:
    ...
```

学生 HyDE 假设答案可强调：

- 课程核心内容。
- 必要基础。
- 学习难点。

教师 HyDE 可强调：

- 教学目标。
- 培养方案定位。
- 考核和内容覆盖。

访客 HyDE 可强调：

- 专业定位。
- 通俗课程概述。

工程约束：

- 使用功能开关 `PERSONA_HYDE_ENABLED`。
- 缓存 `(query, persona, model, prompt_version)`。
- LLM 不可用时直接使用原查询。
- 记录额外延迟和 token 成本。
- HyDE 文本只用于检索，不作为回答证据。
- 不允许 HyDE 生成的虚构内容出现在最终引用中。

如果 HyDE 对指标没有稳定提升，应在论文中作为消融结果，而不是强行保留到
生产链路。

### 4.6 日志与实验字段

扩展 `build_query_log_record`：

```json
{
  "persona": "teacher",
  "persona_mode": "retrieval",
  "experiment_id": "persona-ablation-v1",
  "retrieval_profile": "teacher-v1",
  "hyde_enabled": false,
  "query_type": "content",
  "latency_ms": 820,
  "retrieved_source_counts": {
    "syllabus": 6,
    "training_plan": 4,
    "knowledge_graph": 0
  }
}
```

不要在日志中记录推断身份或其他个人画像。persona 是一次查询的显式参数。

反馈日志继续通过 `query_id` 与 persona 实验记录关联，无需重复保存用户问题。

### 4.7 评估设计

#### 4.7.1 消融组

至少比较：

| 组别 | Persona Prompt | Persona Retrieval | Persona HyDE |
|---|---|---|---|
| A 当前基线 | 否 | 否 | 否 |
| B Prompt-only | 是 | 否 | 否 |
| C Retrieval-aware | 是 | 是 | 否 |
| D HyDE 可选组 | 是 | 是 | 是 |

论文主要结论应建立在 A/B/C 上。D 只有在结果稳定时进入主结论。

#### 4.7.2 固定问题集

建议 24–30 个问题，覆盖：

- 课程内容。
- 学分、学时、学期。
- 课程目标。
- 教学进度和难点。
- 考核方式。
- 专业培养方案。
- 课程先修与选课路径。
- 无答案和弱相关问题。

每个问题分别以三种 persona 运行。若使用 30 个问题：

```text
30 questions × 3 personas × 3 main ablations = 270 answers
```

#### 4.7.3 指标

所有 persona 共同指标：

- 事实正确性。
- 证据忠实度。
- 引用正确性。
- 无答案安全性。
- 响应延迟。

Persona 专属指标：

- 角色契合度。
- 内容侧重是否符合角色。
- 术语难度是否适当。
- 信息深度是否适当。
- 行动建议是否有用。

建议使用 1–5 分量表，并给评审器提供明确 rubric。

#### 4.7.4 评审方法

1. 输出随机打乱并隐藏实验组。
2. 使用与生成模型不同的 judge model。
3. LLM judge 输出每个指标的分数和证据句。
4. 抽取至少 20% 样本进行人工复核。
5. 报告均值、标准差、胜率和 bootstrap 置信区间。
6. 单独报告事实忠实度，防止 persona 契合度提升但幻觉增加。

如果无法招募真实多角色用户，可由课程教师、学生和非专业人员各自按同一
rubric 复核小样本；不要声称完成了用户画像研究。

### 4.8 建议二验收标准

#### 工程

- persona 非法值返回 422。
- 不传 persona 时行为稳定。
- 三种 persona 均影响生成提示词。
- retrieval-aware 模式能产生可检测的来源和章节排序差异。
- LLM 不可用时 persona 仍影响模板摘要。
- persona 和实验配置写入查询日志。

#### 质量

- 相对基线，角色契合度有稳定提升。
- 事实正确性和引用正确性不得显著下降。
- 无答案问题不得因 persona 或 HyDE 强行生成内容。
- teacher 的 training_plan 证据占比、student 的教学内容/先修证据占比符合
  预设方向。

---

## 5. 推荐代码改动范围

| 文件 | 建议改动 |
|---|---|
| `src/data_processing/parser_chunker.py` | 提取并结构化课程先修字段 |
| `src/data_processing/graph_builder.py` | 写入可追溯课程依赖边；清理旧快照 |
| `src/online_service/course_dependency_service.py` | 新增课程子图、裁剪、拓扑规划 |
| `src/online_service/dependency_explainer.py` | 区分 course dependency 与 concept dependency |
| `src/online_service/query_router.py` | 传递 persona、应用检索 profile |
| `src/online_service/chroma_retriever.py` | 支持 persona source/section boost |
| `src/online_service/generator.py` | Persona prompt 和 persona fallback |
| `src/online_service/feedback_handler.py` | 日志增加 persona 与实验字段 |
| `src/main.py` | API schema、依赖子图接口、日志参数 |
| `src/templates/demo.html` | persona 选择器、Mermaid 图和路径分层 |
| `tests/test_course_dependency.py` | 子图裁剪、环检测、拓扑层级测试 |
| `tests/test_persona_quality.py` | persona API、检索差异和日志测试 |
| `tests/evals/persona_cases.json` | 固定评估问题和 rubric |

---

## 6. 推荐实施顺序与工作量

| 阶段 | 内容 | 预计工作量 |
|---|---|---:|
| 0 | 依赖字段提取、课程实体对齐、未解析清单 | 2–4 天 |
| 1 | Neo4j 快照同步和课程依赖边 | 1–2 天 |
| 2 | 二跳子图 API、裁剪和 Mermaid 前端 | 1–2 天 |
| 3 | DAG 检查、拓扑分层和选课路径 | 2–3 天 |
| 4 | Persona schema、前端选择和 prompt-only | 1–2 天 |
| 5 | Persona 检索权重、路由和日志 | 2–4 天 |
| 6 | 固定评估集、消融实验和结果分析 | 3–5 天 |
| 7 | Persona-aware HyDE，可选 | 2–3 天 |

不含可选 HyDE，合理工作量约为 **12–22 个开发日**。其中 Mermaid 渲染组件
本身仍是 1–2 天，增加的时间主要来自当前缺失的课程依赖数据、图谱一致性和
评估实验。

---

## 7. 推荐论文叙事

### 系统能力

课程依赖部分可描述为：

> 系统从课程大纲中提取可追溯的显式先修关系，构建以目标课程为中心的有限
> 邻域，并在无环条件下生成分层学习路径。

重点展示：

- 有来源的依赖边。
- 子图裁剪。
- 环检测。
- 并行先修分支。
- 培养方案学期信息。

### 研究贡献

Persona-aware 部分可描述为：

> 在共享知识库和事实约束不变的前提下，通过角色感知的证据重排和回答组织，
> 提升不同用户角色的内容契合度，并通过 prompt-only、retrieval-aware 和
> 可选 HyDE 的消融实验验证各阶段贡献。

该叙事比“为三类用户换三段提示词”更有研究价值，同时避免扩展到需要真实用户
历史数据的画像学习。

---

## 8. 最终建议

1. **先做依赖数据，不要先画空图。**
   当前没有课程级依赖边，必须先把 syllabus 中的明确先修信息结构化。
2. **Mermaid 使用确定性边列表生成。**
   不使用 LLM 生成节点 ID、边或 Mermaid 源码。
3. **把路径规划作为建议一的加分阶段。**
   先实现可靠二跳子图，再增加 DAG 拓扑分层。
4. **Persona 从 prompt-only 开始，但论文结论以 retrieval-aware 为主。**
5. **HyDE 是可选消融项，不是当前已有能力。**
6. **现在就固定评估集和日志字段。**
   否则功能完成后无法证明 persona 的实际价值。

推荐优先级：

```text
依赖数据与图谱一致性
    → Mermaid 二跳子图
    → Persona prompt 基线
    → Persona 检索感知
    → 选课路径规划
    → 消融评估
    → 可选 Persona-aware HyDE
```
