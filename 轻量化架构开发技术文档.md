# zsttSystem ChromaDB 轻量化架构

## 1. 目标

系统面向课程培养方案与教学大纲，提供课程事实、课程内容和跨课程依赖
问答。当前架构以 ChromaDB 作为唯一向量数据库，以本地 Neo4j Community
作为图数据库，以 FastAPI 作为在线服务入口。

设计目标：

- 一套离线数据管线。
- 一份 ChromaDB 向量索引。
- Docker 持久化的本地 Neo4j Community 依赖图。
- 本地持久化和 Docker HTTP 两种 ChromaDB 运行方式。
- 外部服务不可用时明确降级。

## 2. 数据流

```text
data/training_plans/*.xlsx
data/syllabi/**/*.docx
            │
            ▼
        parse stage
            │
            ├── outputs/courses.json
            └── outputs/chunks.json
                     │
          ┌──────────┴──────────┐
          ▼                     ▼
     concept stage          embed stage
          │                     │
          ▼                     ▼
  outputs/concepts.json     ChromaDB
          │
          ▼
      graph stage
          │
          ├── outputs/graph_manifest.json
          └── Neo4j Community
```

## 3. ChromaDB 设计

Collection 默认名为 `zstt_chunks`。每个课程片段对应一条记录：

- `id`：稳定的 `chunk_id`。
- `document`：片段正文。
- `embedding`：SentenceTransformer 生成的归一化向量。
- `metadata`：课程代码、课程名称、章节、源文件和领域元数据 JSON。

Collection 使用 cosine 距离。在线查询将距离转换为 `1 - distance` 分数，
并恢复领域元数据供引用生成。

### 本地模式

```env
CHROMA_MODE=local
VECTOR_DB_PATH=chroma_data
```

Python 进程通过 `PersistentClient` 直接访问。

### HTTP 模式

```env
CHROMA_MODE=http
CHROMA_HOST=127.0.0.1
CHROMA_PORT=8001
```

Docker Compose 运行固定版本的 ChromaDB，离线管线和在线 API 均通过
`HttpClient` 访问同一个 collection。

## 4. 在线路由

| 类型 | 检索路径 |
| --- | --- |
| fact | 课程结构化字段，失败后查询 ChromaDB |
| content | ChromaDB top-k |
| dependency | Neo4j |
| hybrid | ChromaDB + Neo4j + LLM |

ChromaDB collection 为空时返回 `VECTOR_INDEX_UNAVAILABLE`。Neo4j 不可用
时 dependency 查询返回显式降级信息。LLM 不可用时 hybrid 返回原始证据。

## 5. 一致性

- 离线 `embed` 每次重建 collection，自动清理已删除片段。
- 索引记录 embedding 模型和维度。
- 在线服务验证查询模型与索引模型一致。
- 人工修正片段后，维护脚本同步更新 JSON 并重建 collection。
- `chroma_data/`、运行日志和临时生成物不进入 Git。

## 6. 部署

```bash
docker compose up -d --wait chromadb neo4j
python run_pipeline.py --stage all
python -m uvicorn src.main:app --host 127.0.0.1 --port 8000
```

ChromaDB 和 Neo4j 端口均只绑定到 `127.0.0.1`。Neo4j 数据保存在
`neo4j_data` 命名卷中。启动脚本等待 Neo4j 健康检查成功后再启动 API。
如需公网部署，应通过带认证和 TLS 的反向代理访问，不能直接暴露数据库。

## 7. 验证标准

- ChromaDB 真实写入和查询集成测试通过。
- 管线 stage map 只包含当前支持阶段。
- API 生命周期测试可在 Neo4j 不可用时启动。
- Python 3.11/3.12 CI 通过编译、关键静态检查和测试。
- README、环境模板、Docker Compose 和实现保持一致。
